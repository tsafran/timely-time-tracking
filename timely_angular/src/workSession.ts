export interface WorkSession {
    id: number;
    name: string;
    startTime: string;
    endTime: string;
}

export interface WorkSessionRow {
    id: number;
    name: string;
    startTime: string;
    endTime: string;
    duration: string;
}